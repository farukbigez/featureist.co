.white-bg
    background var(--white)

.lightGreen-bg
    background var(--lightGreen)

.lightBlue-bg
    background var(--lightBlue)

.color-black
    color var(--black)

.color-notActive
    color var(--notActive)
