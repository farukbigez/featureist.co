.footer
    display flex
    align-items center
    justify-content space-between
    height 120px
    width 100%
    margin-top 30px
    .link-group
        padding 40px 180px
        a
            font-size 18px
            font-weight 600
        .link-name
            cursor pointer
            color var(--steel)